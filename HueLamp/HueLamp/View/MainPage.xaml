<Page
    x:Class="HueLamp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueLamp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="HUEHEUEHUEApp" HorizontalAlignment="Center"/>

            <!-- replace with Lamps-->
            <TextBlock Text="{x:Bind ViewModel.DEFAULTLAMP.test1, Mode=OneWay}"/>
        </StackPanel>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <!-- These commands appear on the right -->
            <AppBarButton Icon="Phone" Label="Connect" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
