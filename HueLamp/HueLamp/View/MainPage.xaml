<Page
    x:Class="HueLamp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueLamp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="HUEHEUEHUEApp" HorizontalAlignment="Center"/>
            <!-- replace with Lamps-->
            <TextBlock Text="Nothing here" x:Name="blah"/>

            <ListView ItemsSource="{Binding Path=Lamps}" Grid.Row="1" Height="500" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollMode="Enabled">
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind Lamps}" FontSize="48" DoubleTapped="TextBlock_DoubleTapped"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <!-- These commands appear on the right -->
            <AppBarButton Icon="Sync" Label="Connect" Click="AppBarButton_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
